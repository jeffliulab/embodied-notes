# Calibration and System Integration

## Overview

Calibration is the foundation of robot perception and manipulation accuracy. System integration connects sensors, controllers, and actuators through a unified coordinate framework and communication architecture into a complete system. This article covers camera calibration, hand-eye calibration, multi-sensor extrinsic calibration, and system integration under ROS2.

!!! info "Accuracy Chain"
    Final manipulation accuracy = Camera intrinsic accuracy x Hand-eye calibration accuracy x Kinematic calibration accuracy x Control accuracy. Errors at any link accumulate to the end-effector.

---

## I. Camera Intrinsic Calibration

### 1.1 Pinhole Camera Model

A camera projects 3D world points $P_w = [X, Y, Z]^T$ to 2D pixels $p = [u, v]^T$:

$$
s \begin{bmatrix} u \\ v \\ 1 \end{bmatrix} = K [R | t] \begin{bmatrix} X \\ Y \\ Z \\ 1 \end{bmatrix}
$$

where the intrinsic matrix:

$$
K = \begin{bmatrix} f_x & 0 & c_x \\ 0 & f_y & c_y \\ 0 & 0 & 1 \end{bmatrix}
$$

- $f_x, f_y$: focal length (pixel units)
- $c_x, c_y$: principal point coordinates
- Distortion parameters: $k_1, k_2, p_1, p_2, k_3$ (radial + tangential)

### 1.2 Zhang's Calibration Method

Zhang's method uses a planar checkerboard and is the most widely used camera calibration method:

**Algorithm Flow:**

1. **Capture multiple checkerboard images** (different angles, 15-25 recommended)
2. **Detect corners**: Sub-pixel accuracy corner extraction
3. **Compute homography matrices**: One homography matrix $H_i$ per image
4. **Solve intrinsics**: Using homography matrix constraints
5. **Solve extrinsics**: Decompose each $H_i$ using intrinsics
6. **Nonlinear optimization**: Minimize reprojection error

$$
\min_{K, k_1, k_2, R_i, t_i} \sum_{i=1}^{n} \sum_{j=1}^{m} \| p_{ij} - \hat{p}(K, k_1, k_2, R_i, t_i, P_j) \|^2
$$

!!! tip "Calibration Tips"
    - Checkerboard should cover all image regions
    - Angle variation should be as large as possible (tilt +-45 deg)
    - Reprojection error should be < 0.5 pixel (good) or < 0.3 pixel (excellent)
    - Calibration board flatness is critical (aluminum or glass substrate printing recommended)

---

## II. Hand-Eye Calibration

### 2.1 Problem Definition

Hand-eye calibration solves for the fixed transform between the camera and the robot end-effector.

**Eye-in-Hand configuration** (camera fixed on arm end):

$$
A_i X = X B_i
$$

where:
- $A_i = T_{end}^{(i+1)^{-1}} T_{end}^{(i)}$: End-effector transform between two motions
- $B_i = T_{cam}^{(i+1)} T_{cam}^{(i)^{-1}}$: Camera transform between two motions
- $X = T_{end}^{cam}$: Hand-eye transform to solve

### 2.2 Tsai-Lenz Solution

Tsai-Lenz (1989) is a classic two-step solution:

**Step 1: Solve rotation** using axis-angle parameterization with half-angle formula:

$$
(\text{skew}(n_{A_i} + n_{B_i})) n_X' = n_{B_i} - n_{A_i}
$$

**Step 2: Solve translation:**

$$
(R_{A_i} - I) t_X = R_X t_{B_i} - t_{A_i}
$$

### 2.3 Other Hand-Eye Calibration Methods

| Method | Year | Features | Accuracy |
|--------|------|----------|----------|
| Tsai-Lenz | 1989 | Separate rotation and translation | Medium |
| Park-Martin | 1994 | Exploits Lie group structure | Higher |
| Daniilidis (dual quaternion) | 1999 | Simultaneous rotation and translation | High |
| Shah (iterative optimization) | 2013 | Nonlinear optimization | Highest |

!!! note "Practical Recommendations"
    - Collect 15-25 groups of data at different poses
    - Motions should include variation in all 6 DOF
    - Avoid small-angle motions (<10 deg); use larger angle changes
    - Cross-validate using multiple methods

---

## III. Multi-Sensor Extrinsic Calibration

### 3.1 LiDAR-Camera Extrinsic Calibration

Solve the transform $T_{lidar}^{cam}$ from LiDAR frame to camera frame.

**Target-based methods:**
1. Use targets with known geometric features (checkerboard, circular targets)
2. Detect targets separately in camera and LiDAR
3. Establish correspondences, minimize registration error

**Targetless methods:**
- Edge alignment: Project LiDAR points to image, align edges
- Mutual Information: Maximize statistical correlation between modalities
- Learning-based: CalibNet, LCCNet, etc.

### 3.2 IMU-Camera Extrinsic Calibration

Kalibr is the de facto standard tool:
- Uses AprilGrid targets
- Continuous motion data collection (sufficiently exciting all axes)
- Joint optimization of IMU extrinsics, time offset, IMU intrinsics

$$
\min_{T_{imu}^{cam}, t_d, b_a, b_g} \sum_k \left[ \|e_{cam}^k\|^2 + \|e_{imu}^k\|^2 \right]
$$

### 3.3 Calibration Pipeline

The geometric essence of hand-eye calibration is a **transform loop $AX = XB$**. The figure below draws this equation as a real coordinate-frame quadrilateral: moving the arm two steps gives the end-effector transform $A$, the synchronized observation of the calibration board gives the camera transform $B$, and the unknown fixed hand-eye transform $X = T_{end}^{cam}$ "aligns" these two edges — walking once around this quadrilateral back to the origin gives the constraint $AX = XB$. This is exactly the geometric object that §2.2 Tsai-Lenz solves by first solving rotation, then translation.

<!-- SVG-DESIGN-NOTES
Type: B (geometric / mathematical intuition — hand-eye calibration AX=XB transform-loop quadrilateral)
Q0 (One Big Idea): the X solved by hand-eye calibration is the fixed transform aligning "end-effector motion A" with "camera observation motion B"; the constraint AX=XB is equivalent to walking once around a coordinate-frame quadrilateral and returning to the origin (the loop is identity)
Q1 (geometric mapping): a four-corner loop — end_t / end_(t+1) / cam_t / cam_(t+1) four small triads (x red, y green, z blue), edges A = end two-step transform, B = camera two-step transform, X = unknown hand-eye transform (the two opposite edges identical), loop arrows + AX=XB in the center
Q2 (DNA): drop the title — four triads + a loop quadrilateral annotated A/B/X + AX=XB is the visual fingerprint of hand-eye calibration, completely unlike a "calibration steps fan-out box diagram"
Q3 (decoration removed): deleted the original 9 equal-size calibration-step boxes + 8 arrows (a step list does not carry the core mathematical proposition "AX=XB geometric constraint"); no legend, A/B/X labeled directly on the corresponding edges
Q4 (direct labels): A/B/X labeled at the midpoints of the corresponding edges; each frame drawn as a triad with its frame name; the constraint AX=XB labeled in the center
Q5 (Dark + bilingual): all var(--dia-*); A edge in accent, B edge in blue, X edge in green, axes use accent/green/blue for xyz; Chinese labels swapped in the EN version
-->
<div class="diagram">
<svg viewBox="0 0 640 400" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Hand-eye calibration AX=XB transform loop">
  <defs>
    <marker id="cal-a" markerWidth="9" markerHeight="9" refX="8" refY="3" orient="auto"><path d="M0,0 L0,6 L8,3 z" fill="var(--dia-stroke-soft)"/></marker>
  </defs>
  <text x="320" y="28" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-weight="600" font-size="17" fill="var(--dia-stroke)">Hand-Eye Calibration — AX = XB Transform Loop</text>

  <g>
    <line x1="140" y1="110" x2="170" y2="110" stroke="var(--dia-accent)" stroke-width="2"/>
    <line x1="140" y1="110" x2="140" y2="80" stroke="var(--dia-green)" stroke-width="2"/>
    <line x1="140" y1="110" x2="122" y2="92" stroke="var(--dia-blue)" stroke-width="2"/>
    <text x="120" y="128" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-stroke)">end_t</text>
  </g>
  <g>
    <line x1="500" y1="110" x2="530" y2="110" stroke="var(--dia-accent)" stroke-width="2"/>
    <line x1="500" y1="110" x2="500" y2="80" stroke="var(--dia-green)" stroke-width="2"/>
    <line x1="500" y1="110" x2="482" y2="92" stroke="var(--dia-blue)" stroke-width="2"/>
    <text x="520" y="128" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-stroke)">end_(t+1)</text>
  </g>
  <g>
    <line x1="140" y1="300" x2="170" y2="300" stroke="var(--dia-accent)" stroke-width="2"/>
    <line x1="140" y1="300" x2="140" y2="270" stroke="var(--dia-green)" stroke-width="2"/>
    <line x1="140" y1="300" x2="122" y2="282" stroke="var(--dia-blue)" stroke-width="2"/>
    <text x="118" y="320" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-stroke)">cam_t</text>
  </g>
  <g>
    <line x1="500" y1="300" x2="530" y2="300" stroke="var(--dia-accent)" stroke-width="2"/>
    <line x1="500" y1="300" x2="500" y2="270" stroke="var(--dia-green)" stroke-width="2"/>
    <line x1="500" y1="300" x2="482" y2="282" stroke="var(--dia-blue)" stroke-width="2"/>
    <text x="522" y="320" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-stroke)">cam_(t+1)</text>
  </g>

  <line x1="180" y1="110" x2="470" y2="110" stroke="var(--dia-accent)" stroke-width="2" marker-end="url(#cal-a)"/>
  <text x="325" y="100" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="13" fill="var(--dia-accent)">A = end-effector two-step motion</text>

  <line x1="180" y1="300" x2="470" y2="300" stroke="var(--dia-blue)" stroke-width="2" marker-end="url(#cal-a)"/>
  <text x="325" y="320" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="13" fill="var(--dia-blue)">B = camera two-step motion</text>

  <line x1="140" y1="140" x2="140" y2="270" stroke="var(--dia-green)" stroke-width="2.4" stroke-dasharray="6 4" marker-end="url(#cal-a)"/>
  <text x="92" y="205" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="13" fill="var(--dia-green)" transform="rotate(-90 92 205)">X = T_end^cam</text>

  <line x1="500" y1="140" x2="500" y2="270" stroke="var(--dia-green)" stroke-width="2.4" stroke-dasharray="6 4" marker-end="url(#cal-a)"/>
  <text x="552" y="205" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="13" fill="var(--dia-green)" transform="rotate(-90 552 205)">X (same hand-eye)</text>

  <text x="320" y="200" text-anchor="middle" font-family="JetBrains Mono, monospace" font-weight="600" font-size="20" fill="var(--dia-stroke)">A·X = X·B</text>
  <text x="320" y="222" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="11" fill="var(--dia-stroke-soft)">loop around = identity ⇒ Tsai-Lenz solves rotation then translation</text>
</svg>
</div>
<p class="figure-caption">Figure 1 — The geometric essence of hand-eye calibration is the transform loop AX=XB: moving the arm two steps gives the end-effector transform A, the synchronized camera observation gives B, and the unknown fixed hand-eye transform X appears on both opposite edges of the quadrilateral. Walking once around this coordinate-frame quadrilateral must return to the origin (the loop is identity) — this is the object solved by the §2.2 Tsai-Lenz two-step method.</p>


---

## IV. System Integration Under ROS2

### 4.1 tf2 Transform Tree

ROS2 uses tf2 to manage transform relationships between all coordinate frames:

```
world
  +-- base_link
       +-- base_footprint
       +-- joint_1
       |    +-- link_1
       |         +-- joint_2
       |              +-- link_2
       |                   +-- ... 
       |                        +-- end_effector
       |                             +-- camera_link
       |                                  +-- camera_color_optical_frame
       |                                  +-- camera_depth_optical_frame
       +-- lidar_link
```

### 4.2 Static and Dynamic Transforms

**Static transforms** (calibration results, do not change over time):

```python
# Publish hand-eye calibration result as static transform
from tf2_ros import StaticTransformBroadcaster

static_broadcaster = StaticTransformBroadcaster(node)
t = TransformStamped()
t.header.frame_id = 'end_effector'
t.child_frame_id = 'camera_link'
t.transform.translation.x = 0.05  # From calibration
t.transform.translation.y = 0.0
t.transform.translation.z = 0.03
t.transform.rotation = quaternion_from_euler(0, -pi/2, 0)
static_broadcaster.sendTransform(t)
```

### 4.3 System Integration Architecture

<!-- SVG-DESIGN-NOTES
Type: A (structure / architecture — ROS2 tf2 coordinate-frame transform tree)
Q0 (One Big Idea): the core data structure of system integration is the tf2 transform tree — all sensor/link frames hang on a single tree rooted at world; edges split into static transforms (calibration results, constant) and dynamic transforms (joint states, real-time at 100-500Hz); a query between any two frames = chained multiplication along the tree path
Q1 (geometric mapping): a real tree topology (not a fan-out of boxes) — root world at top, base_link below carries the kinematic chain joint→link down to end_effector→camera_link (static edge = hand-eye calibration), with lidar_link hanging off the side; static edges solid, dynamic edges dashed, nodes indented by depth
Q2 (DNA): drop the title — a coordinate-frame tree rooted at the top, descending tier by tier along the kinematic chain, with different line styles for static/dynamic edges, is the visual fingerprint of a tf2 frame tree (matching the ASCII tree in the text), completely unlike a "sensor→driver→module" fan-out box diagram
Q3 (decoration removed): deleted the original 13 equal-size boxes + 8 crossing arrows (a data-flow fan-out does not carry the core structure "frame tree + static/dynamic transforms"); no legend, static/dynamic distinguished by line style not color blocks
Q4 (direct labels): each frame name labeled at its node; key edges labeled directly "static (hand-eye calibration)"/"dynamic (joint)"; the root world emphasized larger
Q5 (Dark + bilingual): all var(--dia-*); root/kinematic-chain nodes in stroke, camera branch in accent, lidar branch in blue, static edges solid dynamic edges dashed; Chinese labels swapped in the EN version
-->
<div class="diagram">
<svg viewBox="0 0 700 460" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="ROS2 tf2 coordinate-frame transform tree">
  <defs>
    <marker id="tf-a" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto"><path d="M0,0 L0,6 L7,3 z" fill="var(--dia-stroke-soft)"/></marker>
  </defs>
  <text x="350" y="26" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-weight="600" font-size="17" fill="var(--dia-stroke)">tf2 Frame Transform Tree (solid = static calibration, dashed = dynamic joint)</text>

  <ellipse cx="120" cy="60" rx="46" ry="22" fill="none" stroke="var(--dia-stroke)" stroke-width="2.2"/>
  <text x="120" y="64" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="12" font-weight="600" fill="var(--dia-stroke)">world</text>

  <ellipse cx="120" cy="140" rx="46" ry="20" fill="none" stroke="var(--dia-stroke)" stroke-width="1.8"/>
  <text x="120" y="144" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-stroke)">base_link</text>
  <line x1="120" y1="82" x2="120" y2="120" stroke="var(--dia-stroke-soft)" stroke-width="1.5" marker-end="url(#tf-a)"/>
  <text x="180" y="105" text-anchor="start" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="10" fill="var(--dia-stroke-soft)">static (odometry origin)</text>

  <ellipse cx="120" cy="220" rx="46" ry="20" fill="none" stroke="var(--dia-stroke)" stroke-width="1.8"/>
  <text x="120" y="224" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-stroke)">joint_1…link_n</text>
  <line x1="120" y1="160" x2="120" y2="200" stroke="var(--dia-stroke-soft)" stroke-width="1.5" stroke-dasharray="5 3" marker-end="url(#tf-a)"/>
  <text x="180" y="185" text-anchor="start" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="10" fill="var(--dia-stroke-soft)">dynamic (joint state 100-500Hz)</text>

  <ellipse cx="120" cy="300" rx="50" ry="20" fill="none" stroke="var(--dia-stroke)" stroke-width="1.8"/>
  <text x="120" y="304" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-stroke)">end_effector</text>
  <line x1="120" y1="240" x2="120" y2="280" stroke="var(--dia-stroke-soft)" stroke-width="1.5" stroke-dasharray="5 3" marker-end="url(#tf-a)"/>

  <ellipse cx="120" cy="385" rx="48" ry="20" fill="none" stroke="var(--dia-accent)" stroke-width="2"/>
  <text x="120" y="389" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-accent)">camera_link</text>
  <line x1="120" y1="320" x2="120" y2="365" stroke="var(--dia-accent)" stroke-width="1.8" marker-end="url(#tf-a)"/>
  <text x="180" y="346" text-anchor="start" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="10" fill="var(--dia-accent)">static = hand-eye result T_end^cam</text>

  <ellipse cx="380" cy="360" rx="76" ry="18" fill="none" stroke="var(--dia-accent)" stroke-width="1.5"/>
  <text x="380" y="364" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="10" fill="var(--dia-accent)">camera_color_optical</text>
  <ellipse cx="380" cy="410" rx="76" ry="18" fill="none" stroke="var(--dia-accent)" stroke-width="1.5"/>
  <text x="380" y="414" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="10" fill="var(--dia-accent)">camera_depth_optical</text>
  <line x1="168" y1="382" x2="304" y2="364" stroke="var(--dia-accent)" stroke-width="1.4" marker-end="url(#tf-a)"/>
  <line x1="168" y1="392" x2="304" y2="408" stroke="var(--dia-accent)" stroke-width="1.4" marker-end="url(#tf-a)"/>

  <ellipse cx="460" cy="140" rx="46" ry="20" fill="none" stroke="var(--dia-blue)" stroke-width="1.8"/>
  <text x="460" y="144" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-blue)">lidar_link</text>
  <line x1="166" y1="140" x2="412" y2="140" stroke="var(--dia-blue)" stroke-width="1.6" marker-end="url(#tf-a)"/>
  <text x="290" y="130" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="10" fill="var(--dia-blue)">static = LiDAR extrinsic calibration</text>

  <text x="470" y="240" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="12" fill="var(--dia-stroke-soft)">transform query between any two frames</text>
  <text x="470" y="258" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-stroke-soft)">= chained transforms along the tree path</text>
  <text x="470" y="280" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="11" fill="var(--dia-stroke-soft)">T(lidar→camera) = ∏ edges</text>
</svg>
</div>
<p class="figure-caption">Figure 2 — The core data structure of system integration is the tf2 frame tree: all frames hang on a single tree rooted at world, static edges (solid, e.g. hand-eye/LiDAR calibration results) are constant while dynamic edges (dashed, joint states) update in real time at 100-500 Hz. A transform query between any two frames is equivalent to chaining the edge transforms along the tree path — calibration is essentially determining these static edges.</p>


---

## V. Calibration Verification and Maintenance

### 5.1 Verification Methods

| Verification | Method | Pass Criteria |
|-------------|--------|---------------|
| Reprojection error | Calibration board corner projection comparison | < 0.5 pixel |
| Point cloud-image alignment | LiDAR point projection to image | Edge deviation < 3 pixels |
| Grasping accuracy | Known-position object grasping | Position error < 2mm |
| Hand-eye consistency | Multi-pose target localization | Position std dev < 1mm |

### 5.2 Common Troubleshooting

| Symptom | Possible Cause | Solution |
|---------|---------------|----------|
| Fixed-direction grasp offset | Hand-eye calibration translation error | Re-collect data and re-calibrate |
| Position-dependent grasp offset | Inaccurate camera distortion calibration | Increase number of calibration images |
| Point cloud-image misalignment | Extrinsic calibration drift | Check mounting, re-calibrate |
| Large error at distance, accurate close up | Depth calibration issue | Multi-distance depth correction |

---

## Further Reading

- [Sensors](../../01_Robot_Engineering/15_Hardware_Overview/传感器.md) - Sensor principles and selection
- [ROS2 System](../03_Ecosystem/ROS2.md) - ROS2 framework and communication
- Zhang, Z. "A Flexible New Technique for Camera Calibration." IEEE TPAMI, 2000.
- Tsai, R.Y., Lenz, R.K. "A New Technique for Fully Autonomous and Efficient 3D Robotics Hand/Eye Calibration." IEEE TRA, 1989.
- Kalibr: https://github.com/ethz-asl/kalibr
