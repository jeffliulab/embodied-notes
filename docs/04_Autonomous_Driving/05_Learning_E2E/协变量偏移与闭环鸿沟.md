# 协变量偏移与闭环鸿沟

> *Covariate Shift & the Closed-Loop Gap — 行为克隆为什么在驾驶里会"越开越偏直至雪崩",以及业界用三摄像头、合成扰动、闭环训练怎么把它摁住.*
>
> **难度**：Intermediate-Advanced
> **前置知识**：[模仿学习](../../02_Robot_Learning/02_Methods/模仿学习.md)（含 Ross & Bagnell $O(\epsilon T^2)$ 定理与 DAgger）、[自动驾驶范式与方法谱系](../01_Foundations/自动驾驶范式与方法谱系.md)
> **后续阅读**：[端到端驾驶](端到端驾驶.md)、[数据集与闭环评测](../06_Data_Eval/数据集与闭环评测.md)、[强化学习在机器人中的应用](../../02_Robot_Learning/02_Methods/强化学习在机器人中的应用.md)

---

## 1. 术语与定义

### 1.1 协变量偏移 / 输入分布偏移

**协变量偏移**（covariate shift），中文也叫**输入分布偏移**，指的是：

$$
p_{\text{train}}(x) \neq p_{\text{test}}(x), \quad \text{而条件关系 } p(y \mid x) \text{ 不变}
$$

即训练时见到的输入分布与部署时的输入分布不一致，但"给定输入、正确答案是什么"这件事没变。模型在训练分布上学得再好，一旦部署时输入落到训练里罕见/没见过的区域，预测就不可靠。

### 1.2 与近邻概念的区分

| 偏移类型 | 变的是什么 | 驾驶里的例子 |
|---|---|---|
| **协变量偏移** covariate shift | 输入边际 $p(x)$ 变，$p(y\mid x)$ 不变 | 训练在晴天市区，部署到雨夜郊区 |
| 标签偏移 label / prior shift | 标签边际 $p(y)$ 变 | 训练集里"直行"占 90%，部署在弯道密集区 |
| 概念漂移 concept drift | $p(y\mid x)$ 本身随时间变 | 交规改了、车道线重画 |

本篇只谈第一种。关键在于：**在模仿学习/闭环控制里，这个 $p(x)$ 的偏移不是外界强加的，而是策略自己造出来的。**

---

## 2. 两层视角：通用 ML 的偶发 vs IL/闭环的必然

### 2.1 通用监督学习里：外因、偶发

普通 ML 假设数据独立同分布。协变量偏移在这里是**外因偶发**的——采样偏差、地域/季节变化、传感器更换。数据集是**固定**的，模型不会改变自己将来会见到什么。处理手段是重要性加权、域适应等"被动补偿"。

### 2.2 IL / 闭环控制里：内因、结构性必然

模仿学习的策略一旦**上车闭环运行**，情况质变：策略**当前的输出决定了下一时刻它会看到什么状态**。于是策略诱导出**自己的状态访问分布** $d_\pi$，它与专家的 $d_{\pi^*}$ **必然不同**：

$$
d_\pi \neq d_{\pi^*}
$$

这就是**闭环（策略影响自己的输入分布）与开环（数据集固定不动）的根本差异**。在普通监督学习里协变量偏移是"可能发生"，在 IL 闭环里是"一定发生"——只要策略不是和专家逐比特相同，它就会把自己带到专家没演示过的状态。

### 2.3 误差界（引用，不重证）

[模仿学习](../../02_Robot_Learning/02_Methods/模仿学习.md)笔记已给出 Ross & Bagnell 的经典结论：若行为克隆每步出错概率为 $\epsilon$，则 $T$ 步轨迹的总损失上界是 $J(\pi_\theta)-J(\pi^*)\le O(\epsilon T^2)$（**二次**增长），而 DAgger 通过在策略自身访问的状态上查询专家，把它降到 $O(\epsilon T)$（线性）。本篇不重复推导，直接用这个结论解释驾驶。

### 2.4 为何"驾驶里 ε 极小也致命"

二次界只是统计期望。在自动驾驶里有三重放大，使得即便每步误差 $\epsilon$ 极小也可能致命：

- **非线性吸收态 → 正反馈雪崩**：车辆动力学使一次小偏离不是"线性累加的误差"，而是把状态推向训练分布外的"吸收区"（压线、骑上路肩、切入对向车道）。一旦进入策略从未见过的状态，输出近乎随机 → 偏得更远 → 状态更陌生，构成正反馈，误差**指数级**离开专家流形（见 Figure 1）。
- **安全的非对称代价**：监督损失把所有误差等权（偏 0.5 m 和偏 2 m 的 MSE 差不多），但真实世界的代价是**阶跃式**的——偏 0.5 m 还在车道内，偏 2 m 就出界/碰撞。所以"期望误差 $\epsilon$ 很小"会严重**低估尾部风险**。
- **时间尺度**：$\epsilon T^2$ 里的 $T$ 在高速场景对应很大的物理位移，长程任务下 $T$ 绝不可忽略。

<!-- SVG-DESIGN-NOTES
Type: B (几何 — 专家窄带分布 vs 策略轨迹螺旋发散冲出 OOD)
Q0: BC 只在专家分布 d_π*(窄带)上可靠;一步小误差把状态推出窄带→进入未训练区(OOD)→输出更不可靠→更大误差,状态轨迹指数级离开专家流形;这是闭环的正反馈,不是线性累加
Q1: 左侧一条蓝色专家轨迹窄带(d_π*);其上叠一条绿色策略轨迹,起初贴合、随时间向外螺旋发散(每段误差箭头逐步变长);背景浅色等高线=训练数据密度,策略轨迹冲出高密度区即转灰(OOD)
Q2: 去标题:蓝窄带 + 绿螺旋发散 + 冲出处变灰 + 误差箭头渐长 = 协变量偏移雪崩 DNA
Q3: 不画孤立装饰;发散关系用螺旋几何与渐长箭头承载
Q4: 标 d_π*(专家窄带)、d_π(策略)、OOD(策略未见过)、ε→εT² 误差累积
Q5: 专家带 var(--dia-blue);策略轨迹 var(--dia-accent);OOD 区 var(--dia-stroke-tertiary);英文版镜像
-->
<div class="diagram">
<svg viewBox="0 0 720 320" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="专家窄带分布与策略轨迹螺旋发散冲出训练分布的协变量偏移雪崩">
  <defs>
    <marker id="cs-err" markerWidth="8" markerHeight="8" refX="7" refY="3" orient="auto">
      <path d="M0,0 L0,6 L7,3 z" fill="var(--dia-accent)"/>
    </marker>
  </defs>
  <text x="360" y="26" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-weight="600" font-size="17" fill="var(--dia-stroke)">协变量偏移雪崩 — 一步出窄带,正反馈把状态指数级推向 OOD</text>

  <!-- training density contour band (expert narrow band) -->
  <path d="M 60 230 Q 250 215 440 220 Q 580 224 680 230 L 680 270 Q 580 264 440 260 Q 250 255 60 270 Z" fill="var(--dia-accent-soft)" stroke="none"/>
  <path d="M 60 250 Q 250 238 440 240 Q 580 244 680 250" fill="none" stroke="var(--dia-blue)" stroke-width="2.4"/>
  <text x="90" y="285" font-family="JetBrains Mono, monospace" font-size="10" fill="var(--dia-blue)">d_π*  专家窄带 (高数据密度)</text>

  <!-- policy trajectory: starts on band, spirals outward -->
  <path d="M 80 248 Q 200 244 300 235 Q 380 226 430 200 Q 470 176 480 130 Q 486 96 520 78 Q 560 58 620 64"
        fill="none" stroke="var(--dia-accent)" stroke-width="2.2" stroke-dasharray="1 0"/>
  <text x="150" y="232" font-family="JetBrains Mono, monospace" font-size="10" fill="var(--dia-accent-deep)">d_π  策略轨迹</text>

  <!-- error arrows growing -->
  <line x1="300" y1="240" x2="300" y2="233" stroke="var(--dia-accent)" stroke-width="1.2" marker-end="url(#cs-err)"/>
  <line x1="430" y1="244" x2="430" y2="202" stroke="var(--dia-accent)" stroke-width="1.4" marker-end="url(#cs-err)"/>
  <line x1="520" y1="248" x2="520" y2="80" stroke="var(--dia-accent)" stroke-width="1.6" marker-end="url(#cs-err)"/>
  <text x="306" y="226" font-family="JetBrains Mono, monospace" font-size="8.5" fill="var(--dia-stroke-soft)">ε</text>
  <text x="436" y="190" font-family="JetBrains Mono, monospace" font-size="8.5" fill="var(--dia-stroke-soft)">↑误差</text>
  <text x="526" y="120" font-family="JetBrains Mono, monospace" font-size="8.5" fill="var(--dia-stroke-soft)">≈ εT²</text>

  <!-- OOD region (gray, above the band) -->
  <rect x="440" y="60" width="250" height="95" fill="var(--dia-stroke-tertiary)" opacity="0.14" stroke="var(--dia-stroke-tertiary)" stroke-dasharray="4 3"/>
  <text x="565" y="80" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="11" fill="var(--dia-stroke-soft)">OOD：策略从未见过</text>
  <text x="565" y="98" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="9" fill="var(--dia-stroke-soft)">输出近乎随机 → 正反馈雪崩</text>

  <text x="360" y="308" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="11" fill="var(--dia-stroke-soft)">闭环里策略输出决定下一状态 → d_π 必然偏离 d_π*；冲出窄带后误差不再线性而是雪崩</text>
</svg>
</div>
<p class="figure-caption">Figure 1 — 协变量偏移在闭环里的雪崩：策略轨迹（橙）起初贴着专家窄带（蓝，高数据密度）行驶，但每步小误差把它向外推；一旦冲出训练分布进入 OOD 区（灰），输出近乎随机，误差从线性变为正反馈式指数发散。这正是 $d_\pi\neq d_{\pi^*}$ 在物理闭环里的几何图像。</p>

---

## 3. 开环评估 vs 闭环评估的鸿沟

协变量偏移最隐蔽的后果，是它让**离线评估系统性地高估**模型的真实表现。

### 3.1 开环（离线回放）评估

把模型放在**专家录制的轨迹**上，逐帧比对它预测的动作与专家动作（指标如 L2 位移误差、动作分类准确率）。注意：状态推进始终由**专家**驱动，模型**从不真正开车**——它永远待在 $d_{\pi^*}$ 上，协变量偏移根本没机会发生。结果是**系统性乐观**。

### 3.2 闭环（仿真/上路）评估

让模型**实际驱动**车辆，下一时刻的状态由它**自己的动作**演化而来。这才暴露 $d_\pi\neq d_{\pi^*}$ 与误差累积，指标是成功率、碰撞率、违规分、接管次数。

### 3.3 为什么离线指标好 ≠ 上路好

Codevilla 等 (2018) 的关键实证发现：**开环 L2 误差与闭环驾驶成功率只有弱相关，甚至会出现反相关**——一个开环 L2 更低的模型，上路成功率反而更差。原因正是开环评估把模型锁在专家分布上，量不到它脱离分布后会怎样。**只看离线指标选模型，会被协变量偏移系统性地误导。**

<!-- SVG-DESIGN-NOTES
Type: A (对比 — 开环回放 vs 闭环自驱 两条流程)
Q0: 开环=策略在固定专家轨迹上"对答案",状态始终由专家驱动,永不离专家分布→乐观 L2;闭环=策略输出反馈进状态更新回路,误差累积,暴露协变量偏移→真实碰撞/出界;同一模型两种评估给出截然不同结论
Q1: 上下两条流程线.上(开环):专家轨迹固定实线,策略每点旁打一个"预测对比"叉,状态仍由专家推进(箭头来自专家);下(闭环):策略输出经一条自环箭头反馈进状态更新,轨迹逐步偏离专家.中间一条竖虚线标 GAP
Q2: 去标题:上=直线+旁挂对比叉(不闭合),下=带自环反馈的偏离轨迹,中缝 GAP = 开环/闭环鸿沟 DNA
Q3: 不画装饰;开环"不闭合"用流程不回环承载,闭环用自环箭头承载
Q4: 标"开环:回放专家(乐观 L2 低)""闭环:策略自驱(碰撞/出界)""指标:L2 vs 成功率/碰撞率"
Q5: 开环 var(--dia-green);闭环 var(--dia-accent-deep);GAP 线 var(--dia-gold);英文版镜像
-->
<div class="diagram">
<svg viewBox="0 0 760 300" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="开环回放评估与闭环自驱评估的鸿沟对比">
  <defs>
    <marker id="cs-g" markerWidth="9" markerHeight="9" refX="8" refY="3" orient="auto"><path d="M0,0 L0,6 L8,3 z" fill="var(--dia-green)"/></marker>
    <marker id="cs-a" markerWidth="9" markerHeight="9" refX="8" refY="3" orient="auto"><path d="M0,0 L0,6 L8,3 z" fill="var(--dia-accent-deep)"/></marker>
  </defs>
  <text x="380" y="26" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-weight="600" font-size="17" fill="var(--dia-stroke)">同一模型,两种评估,两种结论</text>

  <!-- OPEN-LOOP (top) -->
  <text x="40" y="78" font-family="Fraunces, Georgia, serif" font-weight="700" font-size="13" fill="var(--dia-green)">开环 (回放专家)</text>
  <text x="40" y="94" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="9" fill="var(--dia-stroke-soft)">状态由专家推进</text>
  <line x1="220" y1="90" x2="560" y2="90" stroke="var(--dia-green)" stroke-width="2" marker-end="url(#cs-g)"/>
  <g fill="var(--dia-green)"><circle cx="260" cy="90" r="4"/><circle cx="360" cy="90" r="4"/><circle cx="460" cy="90" r="4"/></g>
  <!-- prediction-compare ticks hanging off, not closing the loop -->
  <g stroke="var(--dia-stroke-soft)" stroke-width="1">
    <line x1="260" y1="90" x2="260" y2="112"/><line x1="360" y1="90" x2="360" y2="112"/><line x1="460" y1="90" x2="460" y2="112"/>
  </g>
  <text x="360" y="126" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="9" fill="var(--dia-stroke-soft)">逐帧对比预测动作 vs 专家 (L2)</text>
  <text x="600" y="94" font-family="JetBrains Mono, monospace" font-size="10" fill="var(--dia-green)">L2 低 → 乐观</text>

  <!-- GAP divider -->
  <line x1="380" y1="150" x2="380" y2="210" stroke="var(--dia-gold)" stroke-width="1.6" stroke-dasharray="5 4"/>
  <text x="380" y="170" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-weight="700" font-style="italic" font-size="13" fill="var(--dia-gold)">GAP</text>

  <!-- CLOSED-LOOP (bottom) -->
  <text x="40" y="238" font-family="Fraunces, Georgia, serif" font-weight="700" font-size="13" fill="var(--dia-accent-deep)">闭环 (策略自驱)</text>
  <text x="40" y="254" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="9" fill="var(--dia-stroke-soft)">状态由策略动作演化</text>
  <path d="M 220 250 Q 320 250 380 244 Q 450 236 500 218 Q 540 204 575 196" fill="none" stroke="var(--dia-accent-deep)" stroke-width="2" marker-end="url(#cs-a)"/>
  <!-- self-loop feedback arrow -->
  <path d="M 360 250 C 360 285 280 285 280 256" fill="none" stroke="var(--dia-accent)" stroke-width="1.4" marker-end="url(#cs-a)"/>
  <text x="320" y="298" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="8.5" fill="var(--dia-accent)">动作→状态 反馈</text>
  <text x="600" y="200" font-family="JetBrains Mono, monospace" font-size="10" fill="var(--dia-accent-deep)">偏离→碰撞/出界</text>
</svg>
</div>
<p class="figure-caption">Figure 2 — 开环 vs 闭环评估鸿沟：开环（上，绿）让模型在固定专家轨迹上"对答案"，状态由专家推进，永不离分布，L2 看起来很低；闭环（下，橙）让模型自己开，动作反馈进状态更新（自环箭头），误差累积导致偏离与碰撞。两者的差距（GAP）正是协变量偏移的度量。</p>

### 3.4 闭环 leaderboard

诊断协变量偏移的唯一可信尺子是**闭环评测**：

- **CARLA Leaderboard**：在 CARLA 仿真城市里跑预设路线，计驾驶分（行驶完成度 × 违规惩罚：闯红灯、碰撞、压线各扣分）。
- **nuPlan**：首个大规模**真实数据**闭环规划基准，区分非反应式（背景交通按 log 回放）与反应式（背景交通对本车反应）两种闭环，更贴近真实交互。

详见[数据集与闭环评测](../06_Data_Eval/数据集与闭环评测.md)。

---

## 4. 解决方法谱系

所有解法本质上都是**想办法让训练分布覆盖到策略实际会访问的 $d_\pi$**，区别只在"用什么方式逼近 $d_\pi$、要不要在线专家、要不要仿真"。

<!-- SVG-DESIGN-NOTES
Type: A (结构 — 两条把 recovery 数据塞进训练分布的工程路线)
Q0: 把"偏离-纠回"样本塞进训练分布的两条经典工程路线——NVIDIA 三摄像头视角合成(空间偏置)与 ChauffeurNet 轨迹扰动+辅助损失(时序偏置)——本质都是人造 d_π 覆盖,无需在线查询专家
Q1: 左半俯视车道,车上画左/中/右三个摄像头视锥,左右摄像头各配一个"应向右/左纠正"的标注转向;右半画一条专家轨迹被扰动出一条偏移轨迹+一条标注的纠回箭头,下方两个辅助损失图标(碰撞/离路罚)
Q2: 去标题:左三视锥+纠正转向标注,右专家轨迹被扰动+纠回箭头+罚项 = 人造 recovery 数据 DNA
Q3: 不画装饰;recovery 用"偏置视角/扰动+纠回"几何承载
Q4: 标"三摄像头→合成 recovery""轨迹扰动→学纠正""辅助损失:碰撞/离路罚"
Q5: 车道 var(--dia-stroke-soft);视锥 var(--dia-blue);扰动/纠回 var(--dia-accent);罚项 var(--dia-gold);英文版镜像
-->
<div class="diagram">
<svg viewBox="0 0 720 300" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="三摄像头视角合成与轨迹扰动两种人造 recovery 数据路线">
  <defs>
    <marker id="cs-r" markerWidth="9" markerHeight="9" refX="8" refY="3" orient="auto"><path d="M0,0 L0,6 L8,3 z" fill="var(--dia-accent)"/></marker>
  </defs>
  <text x="360" y="26" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-weight="600" font-size="17" fill="var(--dia-stroke)">两条人造 recovery 数据路线 — 不查在线专家也能覆盖 d_π</text>

  <!-- LEFT: 3-camera -->
  <text x="170" y="58" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-weight="700" font-size="12" fill="var(--dia-blue)">NVIDIA 三摄像头视角合成</text>
  <!-- lane -->
  <line x1="60" y1="90" x2="60" y2="250" stroke="var(--dia-stroke-soft)" stroke-width="1.4"/>
  <line x1="290" y1="90" x2="290" y2="250" stroke="var(--dia-stroke-soft)" stroke-width="1.4"/>
  <line x1="175" y1="90" x2="175" y2="250" stroke="var(--dia-stroke-soft)" stroke-width="1" stroke-dasharray="6 6"/>
  <!-- car -->
  <rect x="158" y="200" width="34" height="48" rx="4" fill="var(--dia-bg-card)" stroke="var(--dia-stroke)" stroke-width="1.6"/>
  <!-- three view cones -->
  <path d="M 165 200 L 120 120 L 150 120 Z" fill="var(--dia-blue)" opacity="0.18" stroke="var(--dia-blue)" stroke-width="0.8"/>
  <path d="M 175 200 L 162 116 L 188 116 Z" fill="var(--dia-blue)" opacity="0.28" stroke="var(--dia-blue)" stroke-width="0.8"/>
  <path d="M 185 200 L 200 120 L 230 120 Z" fill="var(--dia-blue)" opacity="0.18" stroke="var(--dia-blue)" stroke-width="0.8"/>
  <text x="120" y="135" font-family="JetBrains Mono, monospace" font-size="8" fill="var(--dia-accent-deep)">左摄→标"向右纠"</text>
  <text x="196" y="135" font-family="JetBrains Mono, monospace" font-size="8" fill="var(--dia-accent-deep)">右摄→标"向左纠"</text>
  <text x="170" y="270" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="9" fill="var(--dia-stroke-soft)">偏置视角=人造"偏离"状态+纠回标签</text>

  <!-- divider -->
  <line x1="360" y1="70" x2="360" y2="270" stroke="var(--dia-rule)" stroke-width="1"/>

  <!-- RIGHT: ChauffeurNet perturbation -->
  <text x="540" y="58" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-weight="700" font-size="12" fill="var(--dia-blue)">ChauffeurNet 轨迹扰动 + 辅助损失</text>
  <!-- expert trajectory -->
  <path d="M 420 240 Q 500 220 600 150" fill="none" stroke="var(--dia-green)" stroke-width="2.2"/>
  <text x="430" y="256" font-family="JetBrains Mono, monospace" font-size="9" fill="var(--dia-green)">专家轨迹</text>
  <!-- perturbed trajectory -->
  <path d="M 420 240 Q 510 248 580 210" fill="none" stroke="var(--dia-accent)" stroke-width="2" stroke-dasharray="4 3"/>
  <text x="560" y="226" font-family="JetBrains Mono, monospace" font-size="8.5" fill="var(--dia-accent-deep)">扰动(偏离)</text>
  <!-- recovery arrow -->
  <path d="M 580 210 Q 595 180 600 152" fill="none" stroke="var(--dia-accent)" stroke-width="1.6" marker-end="url(#cs-r)"/>
  <text x="612" y="180" font-family="JetBrains Mono, monospace" font-size="8.5" fill="var(--dia-accent-deep)">学纠回</text>
  <!-- aux loss icons -->
  <rect x="430" y="118" width="120" height="20" rx="3" fill="var(--dia-bg-card)" stroke="var(--dia-gold)" stroke-width="1.2"/>
  <text x="490" y="132" text-anchor="middle" font-family="JetBrains Mono, monospace" font-size="8.5" fill="var(--dia-gold)">辅助损失:碰撞/离路罚</text>
  <text x="540" y="270" text-anchor="middle" font-family="Fraunces, Georgia, serif" font-style="italic" font-size="9" fill="var(--dia-stroke-soft)">主动扰动专家轨迹=训练里"预演灾难"</text>
</svg>
</div>
<p class="figure-caption">Figure 3 — 两条人造 recovery 数据路线：左为 NVIDIA 三摄像头视角合成（用左右偏置摄像头制造"偏离"状态并打上纠回标签，空间偏置）；右为 ChauffeurNet 主动扰动专家轨迹并以辅助损失（碰撞/离路）惩罚危险状态（时序偏置）。两者都在不查询在线专家的前提下，把策略将会遇到的 $d_\pi$ 提前塞进训练分布。</p>

### 4.1 DAgger 及其驾驶变体

核心思想是在**策略自己访问的状态**上查询专家标签，把训练分布主动拉向 $d_\pi$（框架与 $O(\epsilon T)$ 保证见[模仿学习](../../02_Robot_Learning/02_Methods/模仿学习.md)）。驾驶落地的难点是**在线查询专家昂贵且危险**（要人类司机实时盯着纠正）。变体：**SafeDAgger**（只在策略不确定时才查询专家，省标注）、**HG-DAgger**（人在环交互式接管纠正）、或用一个规则/特权专家替代人类专家。

### 4.2 NVIDIA 三摄像头 / 视角合成做 recovery

PilotNet 在车上装左/中/右三个摄像头：中间摄像头配专家转向作正常样本，左右偏置摄像头配上"应当向右/左纠正"的转向标签——人为制造大量"偏离-纠回"样本（再辅以几何视角合成插值更多偏移量）。**用数据而非在线查询**近似了 DAgger 的效果，是工程上最朴素有效的一招（见 Figure 3 左）。

### 4.3 ChauffeurNet 的合成扰动 + 辅助损失

ChauffeurNet 在 mid-to-mid 表示上训练，关键两招：① 训练时**主动扰动专家轨迹**（制造离路、偏移、急刹），逼网络学会从坏状态纠回；② 加**辅助损失**（碰撞、偏离道路、保持在道、几何一致），显式惩罚危险状态。相当于在训练里"预演灾难"，是对 §4.2 思路的进一步系统化（见 Figure 3 右）。

### 4.4 噪声注入 / DART

DART (Laskey et al., 2017) 在收集演示时**向专家动作注入噪声**，使专家自然地访问并演示"偏离-纠回"，从而扩大演示覆盖的状态分布。它在采集阶段近似 DAgger，但**无需在线专家**，比 DAgger 更易落地。

### 4.5 闭环训练 + RL 微调

让策略在**仿真闭环**里自己 rollout，用 RL/奖励（或在线 DAgger）直接修正分布外行为——把"训练分布 = 部署分布"变成现实。这正对应[范式谱系](../01_Foundations/自动驾驶范式与方法谱系.md)里"端到端 RL"象限（如 Roach 用 RL 教练在 CARLA 闭环里训练再蒸馏给 IL 学生）。

### 4.6 数据增广与长尾场景挖掘

主动挖掘长尾/接管/disengagement 场景定向补数据：影子模式（量产车后台跑模型、与人类司机分歧时记录）、难例回灌、对抗场景生成。本质是定向扩充 $d_\pi$ 中的高风险尾部。

### 4.7 世界模型生成长尾

用生成式世界模型（GAIA-1 等）**合成**稀有危险场景，既做 recovery 训练数据，又做闭环评测环境，绕开真实世界采集这些场景的风险与成本。详见[表征与世界模型](../../00_Robotics/01_Embodiment_Theory/表征与世界模型.md)。

| 方法 | 如何逼近 $d_\pi$ | 需在线专家 | 需仿真 | 代表 |
|---|---|---|---|---|
| DAgger / SafeDAgger / HG-DAgger | 在策略访问的状态上查专家 | 是 | 否 | Ross 2011 / 驾驶变体 |
| 三摄像头视角合成 | 空间偏置造 recovery 数据 | 否 | 否 | PilotNet |
| 轨迹扰动 + 辅助损失 | 时序扰动 + 罚危险态 | 否 | 否 | ChauffeurNet |
| 噪声注入 DART | 采集时注入噪声扩分布 | 否 | 否 | DART |
| 闭环训练 + RL 微调 | 直接在 $d_\pi$ 上训 | 否 | 是 | Roach |
| 长尾挖掘 / 影子模式 | 定向补高风险尾部 | 否 | 否 | 工业实践 |
| 世界模型造长尾 | 生成稀有场景 | 否 | 是（生成） | GAIA-1 |

---

## 5. 经典案例串讲：一部"对抗协变量偏移"的进化史

- **ALVINN (1989)**：朴素像素→转向 BC，**没有任何 recovery 机制**，偏离即失效——协变量偏移的原始受害者。
- **PilotNet (2016)**：同样是 E2E+BC，但首次用**三摄像头/视角合成系统性注入 recovery 数据**（§4.2），让车能从偏离中纠回。
- **ChauffeurNet (2018)**：搬到 mid-to-mid 模块化表示，用**合成扰动 + 辅助损失在训练里"预演灾难"**（§4.3），并显式罚危险状态。

一句话收尾：从"无视"→"造数据"→"造灾难 + 罚危险"→"闭环训练 / 世界模型造长尾"，是同一个协变量偏移问题被层层逼近的历史。

---

## 6. 方法对比

见 §4 末尾小结表。一句话排序：**最易落地** 三摄像头/DART/扰动（无需在线专家、无需仿真）→ **最贴近本质** 闭环训练/RL 微调（直接在 $d_\pi$ 上训，但要仿真）→ **覆盖长尾最强** 世界模型生成（但训练/评测成本高）。

---

## 7. Common Pitfalls

1. **只看开环 L2 就上车**：协变量偏移会让离线指标系统性乐观（§3.3），上路前必须闭环评测。
2. **以为加大 BC 数据量就能根治**：扩大 $d_{\pi^*}$ 上的数据治标不治本，因为问题出在 $d_\pi$ 上策略没见过的状态；DAgger/recovery 才是对症。
3. **把 recovery 数据当普通增广**：随机增广若不覆盖真实失败模式（压线、切对向、急刹），对协变量偏移帮助有限。
4. **DAgger 直接上真车**：在线让一个未成熟策略开车采数据有安全风险，需 SafeDAgger/人在环兜底。
5. **闭环评测用非反应式背景交通**：背景车不对本车反应会让分数虚高，真实交互（反应式，如 nuPlan 反应式赛道）才暴露问题。

---

## 8. 前沿趋势

1. **闭环可微仿真 + RL 微调**成为端到端训练标配，从源头消除分布鸿沟。
2. **生成式世界模型造长尾**（GAIA 系）：合成稀有危险场景做闭环训练与评测。
3. **大模型 / VLA 带来语言级可解释纠错**：用语言推理识别并解释"我正在偏离"。
4. **真实-仿真混合闭环评测**：nuPlan / CARLA 之后，把真实日志重演与仿真反应式交通结合，逼近真实分布。

---

## 参考文献

1. Ross, S., Gordon, G., & Bagnell, D. (2011). *A Reduction of Imitation Learning and Structured Prediction to No-Regret Online Learning (DAgger)*. AISTATS.（$O(\epsilon T^2)$/$O(\epsilon T)$ 定理详见[模仿学习](../../02_Robot_Learning/02_Methods/模仿学习.md)）
2. Pomerleau, D. (1989). *ALVINN: An Autonomous Land Vehicle in a Neural Network*. NeurIPS.
3. Bojarski, M., et al. (2016). *End to End Learning for Self-Driving Cars*. arXiv:1604.07316.
4. Bansal, M., Krishnan, A., & Ogale, A. (2018). *ChauffeurNet: Learning to Drive by Imitating the Best and Synthesizing the Worst*. arXiv:1812.03079 / RSS 2019.
5. Laskey, M., Lee, J., Fox, R., Dragan, A., & Goldberg, K. (2017). *DART: Noise Injection for Robust Imitation Learning*. CoRL.
6. Zhang, J., & Cho, K. (2017). *Query-Efficient Imitation Learning for End-to-End Autonomous Driving (SafeDAgger)*. AAAI.
7. Kelly, M., Sidrane, C., Driggs-Campbell, K., & Kochenderfer, M. (2019). *HG-DAgger: Interactive Imitation Learning with Human Experts*. ICRA.
8. Codevilla, F., Lopez, A., Koltun, V., & Dosovitskiy, A. (2018). *On Offline Evaluation of Vision-based Driving Models*. ECCV.（开环-闭环鸿沟关键实证）
9. Caesar, H., et al. (2021). *nuPlan: A Closed-Loop ML-Based Planning Benchmark for Autonomous Vehicles*. arXiv:2106.11810.
10. Dosovitskiy, A., et al. (2017). *CARLA: An Open Urban Driving Simulator*. CoRL.
11. Hu, A., et al. (2023). *GAIA-1: A Generative World Model for Autonomous Driving*. arXiv:2309.17080.
