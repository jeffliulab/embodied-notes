# embodied-notes 导航细化升级计划

> 状态：**已执行**（2026-05-29，分支 `nav-upgrade`）。本文记录本次升级的决策、结构变更、执行步骤与后续事项，供回溯与继续维护。

## 已确认决策

- **参考样板**：`ai-notes`（手动深层 `nav:` + 编号子目录 + 每目录 `index.md` 落地页 + `nav_translations` 中英映射 + KaTeX/Tab 下拉/Giscus 配套）。
- **导航策略**：手动深层 nav，下钻到每一页；用 `navigation.indexes` 让章节 `index.md` 作为落地页。
- **扁平大章节**：能合理拆子目录的就拆（00/02/03 已拆）；小章节保持扁平逐页登记。
- **断链**：本次一并脚本化修复全仓历史断链（旧目录改名遗留），跨仓库链转 `ai-notes` URL。
- **配套**：修复 KaTeX、移植顶部 Tab 下拉、Giscus 评论（占位 ID）、整理 01_RE 冗余、补 Tufte 图色板 CSS。

## 结构变更（重组后）

```
docs/
├── 00_Robotics/                     # 拆 3 子目录
│   ├── 01_Embodiment_Theory/        #   具身智能理论（综述/路线图/里程碑/认知/直觉物理/表征/感知-行动循环）
│   ├── 02_Classical_Robotics/       #   经典机器人学（运动学/动力学/控制/MPC/规划/SLAM…）
│   └── 03_Research_Resources/       #   研究资源（会议期刊/论文精读）
├── 01_Robot_Engineering/            # 去冗余 + 接线（已有 14 子目录 + 新增 15）
│   ├── 01_Computer_Engineering … 11_Integration/
│   ├── 12_Foundations/ 13_Electrical_Engineering/ 14_Manufacturing_Frontiers/   # 修正占位 H1 + 补 index
│   └── 15_Hardware_Overview/        #   新增：传感器/执行器与驱动/计算平台/开源硬件/硬件选型指南
├── 02_Robot_Learning/               # 拆 4 子目录
│   ├── 01_Foundations/              #   综述/基础模型/VLA/路线图/多任务/大模型驱动/世界模型
│   ├── 02_Methods/                  #   模仿学习/扩散策略/RL/【VLA-RL集成（新）】/ACT/Sim2Real
│   ├── 03_Models/                   #   RT-1/RT2-OpenVLA/Octo/π0/RDT/Helix/开源汇总
│   └── 04_Data_Benchmarks/          #   遥操作/数据集/DROID/VLA基准/UMI/Aloha
├── 03_Sim2Real/                     # 拆 3 子目录
│   ├── 01_Simulation/  02_Deployment/  03_Ecosystem/
├── 04_Autonomous_Driving/ … 09_Topics/   # 小章节保持扁平，逐页登记
```

### 删除（01_RE 冗余，含 .en.md 双胞胎）
- `02_Cameras/01_Cameras/`、`02_Cameras/02_LiDAR/`、`02_Cameras/03_IMU_Navigation/`、`02_Cameras/04_Force_Tactile/`（被顶层规范目录取代）
- `02_Cameras/IMU_Detailed.md`、`hardware_platforms.md`（中文槽里的英文误建件，与计算平台/开源硬件重复）

### 内部移动
- 00/02/03 三章共 56 篇正文（含双胞胎 112 个文件）`git mv` 进新子目录。
- 01_RE 顶层 5 篇综述/选型件移入 `15_Hardware_Overview/`。

## 执行步骤（已完成）

0. 建分支 `nav-upgrade`。
1. 删 01_RE 冗余；修 `12/13/14` 占位 H1 并补正式 `index`；建 `15_Hardware_Overview` + index；`01_RE/index` 补 12–15 链接。
2. 为 00/02/03 建子目录，`git mv` 页面成对移入；新建 10 个子目录 `index.md(+.en.md)`。
3. 撰写新笔记 `02_Methods/VLA-RL集成.md(+.en.md)`；在 RL 笔记、多任务笔记各补一处互链。
4. 脚本化修复全仓断链：**821 条按文件名解析到现位置**，**61 条跨仓库转 `ai-notes` URL**，修复一处被坏 find/replace 损坏的链接（`模型→model`），0 条未解析；复核全仓 0 断链。
5. 重写 `mkdocs.yml`：深层 `nav`（246 行）+ `nav_translations`（191 条）；补 `pymdownx.snippets`、`mdx_truly_sane_lists`、`custom_dir: overrides`；修复 KaTeX（新增 `docs/javascripts/katex.js` + auto-render CDN）。
6. 移植配套：`hooks/generate_dropdown.py` + `tab_dropdown.tpl.js` → 生成 `tab_dropdown.js`；`overrides/main.html`（Giscus，占位 ID）；`site_extra_styles/stylesheets/{extra.css, correct_bullet_point.css}`（含 Tufte `--dia-*` 图色板，供既有 SVG 上色）；`deploy.yml` 加 dropdown 生成步骤。
7. 验证：`mkdocs build --strict` **零警告通过**（zh + en 双语，193 个 nav 元素翻译）；双语文件配平 210/210。

## 待办 / 后续

- **Giscus**：`overrides/main.html` 中 `data-repo-id` / `data-category-id` 为占位符；上线前需在 https://giscus.app 为 embodied-notes 的 discussion 仓库生成真实 ID 替换。
- **跨仓库链**：61 条已转为 `https://jeffliulab.github.io/ai-notes/...` URL，假定目标页在 ai-notes 现结构下存在；若 ai-notes 结构调整需复核。
- **可选后续**（本次未做）：移植 `CLAUDE.md` / `NOTE_GUIDE.md` / 笔记撰写规范（11 要素 + Tufte SVG 规范）。

## 风险与注意

- 断链修复以「文件名唯一解析」为主，已用 `mkdocs build --strict` 兜底（任何坏 nav 项 / 内链都会报错）；后续新增/改名笔记后建议重跑 `--strict`。
- 改 `nav` 后需重跑 `python hooks/generate_dropdown.py` 同步顶部下拉（CI 已自动执行）。
- 移动文件用 `git mv` 保留历史；双胞胎 `.md`/`.en.md` 始终成对处理。
